; --- 融化：场上所有 冰(4) 变为 水(1) ---
MeltReaction proc
    mov ecx, 0
@@:
    .if BrickColors[ecx] == 4 ; 冰
        mov BrickColors[ecx], 1 ; 变水
    .endif
    inc ecx
    cmp ecx, 15
    jl @b
    ret
MeltReaction endp